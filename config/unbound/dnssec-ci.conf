# Same as dnssec.conf, for the klutchell/unbound image compose.test.yaml uses in
# CI, which ships its anchor at a different path.
  auto-trust-anchor-file: "/var/unbound/root.key"
  val-clean-additional: yes
