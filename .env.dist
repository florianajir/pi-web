LOCAL_DOMAIN=pi.lanx
IP=192.168.1.30
TIMEZONE=Europe/Paris
EMAIL=admin@example.com
USER=admin
PASSWORD=admin
# BASIC_AUTH no longer configured here; will be generated by `make install` into config/traefik/auth/users using USER & PASSWORD

# --- LAN / DHCP Configuration ---
# Parent network interface name on the host providing LAN connectivity (e.g. eth0, enp3s0)
LAN_PARENT=eth0
# LAN subnet and gateway (match your physical network)
LAN_SUBNET=192.168.1.0/24
LAN_GATEWAY=192.168.1.1

# DHCP range served by Pi-hole (must be inside LAN_SUBNET and not overlap static reservations)
DHCP_START=192.168.1.100
DHCP_END=192.168.1.150
DHCP_ROUTER=192.168.1.1
DHCP_LEASE_TIME=24
